spring:
  datasource:
    platform: postgres
    username: postgres
    password: password
    url: jdbc:postgresql://postgres:5432/postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    properties.hibernate:
      temp.use_jdbc_metadata_defaults: false

application:
  redis:
    host: redis
    port: 6379

logging:
  file: /var/log/spring-boot-demo/app.log